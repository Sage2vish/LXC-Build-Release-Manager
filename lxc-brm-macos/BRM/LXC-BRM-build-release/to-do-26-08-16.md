# to-do-26-08-16

A. Define the build and release workflow for macOS only.
B. Keep release scripts in `scripts/`.
C. Stage logs in `logs/` and final packages in `version/`.
D. Track repo targets and paths in `projects.json`.

# todo-2026-08-16

## Tasks

A. Define the build and release workflow for macOS only.
1. Keep the flow native to Xcode/macOS.
2. Use the release folder as the final output path.

B. Track scripts, logs, and project mapping.
1. Keep shell entry points in `scripts/`.
2. Write logs into `logs/`.
3. Record projects and targets in `projects.json`.

C. Stage the final release package.
1. Place the `.dmg` in `version/`.
2. Keep the version folder as the last stop.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Open | macOS build flow |
| B | Open | Scripts, logs, projects |
| C | Open | Final `.dmg` staging |


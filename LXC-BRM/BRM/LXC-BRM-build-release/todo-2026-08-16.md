# todo-2026-08-16

## Tasks

A. Capture the PDF requirements.
1. Keep build scripts in `scripts/`.
2. Keep build logs in `logs/`.
3. Keep project mapping in `projects.json`.
4. Keep output artifacts in the release flow.

B. Apply the recorded decisions with leverage.
1. Keep the app native and macOS-only.
2. Keep the Xcode root at `LXC-BRM/`.
3. Keep the BRM workspace under `LXC-BRM/BRM/`.
4. Keep the final `.dmg` in `version/`.

C. Track scripts and outputs.
1. `build-ios.sh`
2. `build-android.sh`
3. `release.sh`
4. `build-YYYY-MM-DD-HH-MM-SS.log`
5. `build-YYYY-MM-DD-HH-MM-SS.txt`

D. Track release packaging.
1. Confirm the app build output path.
2. Confirm the final version folder.
3. Confirm the `.dmg` handoff path.
4. Confirm the release notes or version marker if needed.

E. Track requirement-to-decision alignment.
1. Note any requirement that was accepted as-is.
2. Note any requirement that was changed by decision.
3. Note any requirement that remains open.
4. Keep the mismatch visible in context if there is one.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Open | PDF requirements captured |
| B | Open | Decisions with leverage |
| C | Open | Scripts and log names |
| D | Open | Packaging and version output |
| E | Open | Requirement vs decision alignment |


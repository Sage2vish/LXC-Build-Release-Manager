# todo-2026-08-16

## Tasks

A. Define the build and release workflow for macOS only.
1. Keep the flow native to Xcode/macOS.
2. Use the release folder as the final output path.
3. Confirm the app target builds from `LXC-BRM/`.
4. Keep the workflow readable from the README index.

B. Track scripts, logs, and project mapping.
1. Keep shell entry points in `scripts/`.
2. Write logs into `logs/`.
3. Record projects and targets in `projects.json`.
4. Keep script names stable so release steps stay predictable.

C. Stage the final release package.
1. Place the `.dmg` in `version/`.
2. Keep the version folder as the last stop.
3. Preserve older versioned outputs if needed.
4. Make the release handoff obvious in the folder tree.

D. Track release checklist items.
1. Confirm the version number.
2. Confirm the build target.
3. Confirm the output artifact.
4. Confirm the log file path.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Open | macOS build flow |
| B | Open | Scripts, logs, projects |
| C | Open | Final `.dmg` staging |
| D | Open | Release checklist |

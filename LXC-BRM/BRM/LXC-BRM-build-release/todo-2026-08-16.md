# todo-2026-08-16

## PDF Requirements

A. Build scripts
1. Keep build scripts in `scripts/`.
2. Include `build-ios.sh`.
3. Include `build-android.sh`.
4. Include `release.sh`.
5. Keep the scripts callable from the repo path shown in the PDF.

B. Build logs
1. Keep logs in `logs/`.
2. Use timestamped log names like `build-2026-08-16-14-32-45.log`.
3. Preserve `.txt` export support when needed.
4. Keep the log naming pattern consistent.

C. Project mapping
1. Keep `projects.json` in the build-release area.
2. Map repo names to local workspace paths.
3. Map each project to the scripts it uses.
4. Keep the mapping easy to read from the README index.

D. Build output
1. Keep the app output as a `.app` artifact.
2. Keep the output path visible in the build flow.
3. Keep output separate from source folders.

E. Release staging
1. Keep the final release package in `version/`.
2. Place the final `.dmg` there.
3. Keep the version folder as the release handoff point.

## Decisions That Apply

A. Native app root
1. The implementation stays native macOS.
2. The Xcode root is `LXC-BRM/`.

B. Workspace split
1. The BRM workspace lives under `LXC-BRM/BRM/`.
2. Build-release owns scripts, logs, project mapping, and version output.

C. Requirement leverage
1. If a PDF requirement conflicts with a recorded decision, follow the decision.
2. Keep the mismatch visible in context if one exists.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Open | Scripts folder and script names |
| B | Open | Log folder and timestamp format |
| C | Open | `projects.json` mapping |
| D | Open | `.app` output handling |
| E | Open | Final `.dmg` staging |


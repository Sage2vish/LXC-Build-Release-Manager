# worklog-2026-08-16

## Summary

Today the repo was converted into a native macOS-first BRM workspace, the app root was renamed to `LXC-BRM/`, and the folder structure was cleaned so Xcode owns the main workflow.

## Completed Work

- Created the native SwiftUI macOS app shell and verified the Xcode project builds.
- Moved the app container from `lxc-brm-macos/` to `LXC-BRM/`.
- Created BRM folders for shared, frameworks, build-release, worklog, and context.
- Centralized active todo tracking into `LXC-BRM/BRM/LXC-BRM-worklog/todo-2026-08-16.md`.
- Added the context rules file, architecture file, and dated decision log.
- Added the build-release `version/` staging folder for the final `.dmg`.
- Added build-release scripts, logs, and `projects.json`.
- Cleaned out the old Electron/Vite prototype files from the repo root.
- Rebuilt the worklog todo from the PDF requirements and left it open as a working checklist.
- Re-read the actual `LXC-BuildManager.pdf` functional requirements (repo input/detection, build execution, log streaming, project overview, multi-repo support, non-functional targets, v1 scope) and rebuilt `todo-2026-08-16.md` around those sections, since the previous version was generic and did not reflect the real spec.
- Removed empty leftover `todo/` directories from `shared`, `frameworks`, `build-release`, `context`, and `worklog` — filesystem cruft, not tracked in git, that made todos look scattered even though the tracked content was already centralized.
- Corrected the tracking table so nothing is marked `Done` unless the matching code exists; all PDF feature sections (A-G) are `Open` because only the placeholder folder-browser UI exists so far.
- Removed the hardcoded dark gradient background in `ContentView.swift` so the app follows the system's light/dark appearance instead of forcing dark.
- Copied the full functional requirements into `LXC-BRM-context/requirements.md` so they're versioned in-repo instead of only living in an external PDF.
- Rebuilt `todo-2026-08-16.md` around numbered phases (0-7) mapped to the requirements doc's sections, each with its own tracking row.
- Built Phase 1 (Repository Input & Detection): `Repository`/`BuildScript` models, `RepositoryStore` (JSON-persisted recent repos in Application Support, add/remove/pin/select), `BuildScriptScanner` (local `/build/scripts` filesystem scan and GitHub Contents API scan), and a rewritten `ContentView` with a repo sidebar, add-repository sheet (folder picker + GitHub URL field), and a detail view with Build/Logs/History/Overview/Settings tabs — Build tab lists discovered scripts, the other tabs show their owning phase.
- Verified with `xcodebuild -project LXC-BRM.xcodeproj -scheme LXC-BRM -configuration Debug build`: `BUILD SUCCEEDED`, and the app launches.

## Verification

- `xcodebuild -project LXC-BRM/LXC-BRM.xcodeproj -scheme LXC-BRM -configuration Debug build`
- Result: `BUILD SUCCEEDED`
- GitHub sync completed after the rename and cleanup commits.

## Current Focus

- Keep all active todo tracking in the worklog only.
- Update the context files whenever rules, architecture, or decisions change.
- Keep the release flow documented from scripts through version staging.

## Follow-Ups

- Keep the context folder updated whenever rules or architecture change.
- Expand the release checklist as the build and package flow matures.
- Keep the worklog checklist synchronized with the actual release steps.
- Avoid recreating todo files in other BRM folders.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Done | Native macOS app shell and Xcode build verified |
| B | Done | App root renamed to `LXC-BRM/` |
| C | Done | BRM folders, context, and version staging added |
| D | Done | Active todo tracking centralized in worklog |
| E | Open | Keep context updated as decisions change |
| F | Open | Keep release checklist and worklog evolving |

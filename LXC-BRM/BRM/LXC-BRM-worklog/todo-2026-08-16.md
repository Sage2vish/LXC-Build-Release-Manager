# todo-2026-08-16

All active todo tracking lives here, and only here. Source: `LXC-BRM-context/requirements.md` (in-repo copy of `LXC-BuildManager.pdf`), read against `LXC-BRM-context/decisions/decision-2026-08-16.md`.

No other BRM folder keeps a todo file. Work is broken into phases so it ships in reviewable slices instead of one big drop. Phases are built in order; a phase doesn't start until the one before it is at least functional.

`[x]` = built and verified. `[ ]` = not done yet. A box only gets checked when the matching code exists and runs — never for "planned" or "in progress." Verified so far means: compiles (`xcodebuild ... build` → `BUILD SUCCEEDED`) and the logic was traced against a real test repo's `/build/scripts/`. It does **not** yet mean click-through GUI testing — no accessibility/UI automation has been run against the live app.

## Phases

### Phase 0 — Workspace Foundation
- [x] Native Swift/SwiftUI app scaffold under `LXC-BRM/`, Xcode-openable.
- [x] BRM folder layout: shared, frameworks, build-release, worklog, context.
- [x] Rules, architecture, decisions, and requirements captured in `LXC-BRM-context/`.
- [x] Single todo file convention, enforced in this file and the worklog README.
- [x] App follows the system light/dark appearance (no forced color scheme).

### Phase 1 — Repository Input & Detection (requirements.md §1)
- [x] Validate GitHub URL / local path format before accepting it (GitHub URL checked against `github.com` host + owner/repo path before the Add button enables; local path is always valid since it comes from a folder picker).
- [x] Add a repo via a local folder picker (NSOpenPanel) or a pasted GitHub URL.
- [x] Scan the repo root for `/build`; show "No `/build` folder found" when missing.
- [x] Scan `/build/scripts/` for `.sh` files when `/build` exists; show "No build scripts found in `/build/scripts/`" when empty.
- [x] Convert each script filename into a readable label (`build-ios.sh` → "build-ios").
- [x] Persist the repo list to disk (JSON in Application Support) so it survives app restarts.
- [x] Show "last accessed" relative time next to each repo in the sidebar.
- [x] Visible remove control in the sidebar row (✕ icon) and in the repo's Settings tab, not just a right-click menu.
- [x] Support opening and switching between multiple repos in one session; each keeps its own discovered scripts.

### Phase 2 — Build Execution & Management (requirements.md §2)
- [x] Display each discovered script as a clickable command button.
- [x] On click, run `bash /path/to/repo/build/scripts/script-name.sh` as a background subprocess, working directory = repo root.
- [x] Stream stdout/stderr live, line by line, each line timestamped.
- [x] Track status per build: In Progress (spinner), Success/Failed/Cancelled + duration, recorded to history on completion.
- [x] Cancel/Stop control; cancelling preserves partial output and records the run as "Cancelled".

### Phase 3 — Log Storage & Retrieval (requirements.md §3)
- [x] Write a log file to `/build/logs/` per run: `build-YYYY-MM-DD-HH-MM-SS.log`, full stdout+stderr, plain text.
- [x] View current (live) and past build logs from history.
- [x] Monospace, terminal-style log view (dark background, light text) with per-line timestamps, scrollable.
- [x] In-log search: highlight matches, show match count, next/previous navigation.
- [x] Log filters: Errors Only, Warnings Only, Info Only.
- [x] Export current log to a chosen location (defaults to Downloads) as `.log`.

### Phase 4 — Project & Build Overview (requirements.md §4)
- [x] Show repo name, path/URL, connection status, and total builds run (persisted total, not reset per session — see note below).
- [x] List build commands with label and last-run info ("Last run: 2 hours ago ✓" / "Never run") in the Build tab.
- [x] Per-repo build history: script, timestamp, status, duration for every run.
- [x] Compute success rate and average build duration per repo.
- [x] Quick-stats cards: total builds, success rate, average duration, most recently run, last failed build.

### Phase 5 — Multi-Repository Support (requirements.md §5)
- [x] List all opened/recent repos; switching is instant, no loading screen.
- [x] On switch: refresh build buttons, clear log display, reload that repo's stats and history (detail view is keyed to the repo id, so all `@State` resets on switch).
- [x] Keep build history isolated per repo (keyed by repository UUID).
- [x] Add/remove repos.
- [x] Pin/favorite repos; pinned repos sort to the top of the sidebar.

### Phase 6 — Non-Functional Hardening & v1 Scope Check
- [ ] Launch under 2 seconds; repo scan under 5 seconds — not measured with real timing tooling yet.
- [ ] Stress-test graceful error handling for network issues and missing/unreachable repos (basic paths are handled — `.unreachable`/`.missingBuildFolder` states exist — but not adversarially tested).
- [ ] Verify 5-10+ connected repos without visible slowdown.
- [x] Keep the codebase simple, minimal dependencies, easy to extend — no third-party dependencies, Foundation/SwiftUI/AppKit only.
- [x] Offline support: logs/history are local files, viewable with no network; GitHub scan failure is caught and shown as "Unreachable" rather than crashing.
- [x] v1 scope check: repo input+history, `/build` detection, button generation, build execution+log streaming, multi-project support, native `.app`, basic dashboard UI are all present in some form; none of the out-of-scope items (GitHub Actions, webhooks, Slack/email, Docker, Windows/Linux, analytics/charts) were built.

### Phase 7 — Packaging & Deliverables
- [x] Build instructions for packaging the `.app` — written in `LXC-BRM-build-release/README.md`.
- [x] User guide / quick start — written as `LXC-BRM-build-release/USER_GUIDE.md`.
- [x] Configuration template (`projects.json`) kept in `LXC-BRM-build-release/`.
- [ ] Tag a release and stage an actual `.dmg` in `LXC-BRM-build-release/version/` — not done; requires an intentional release build + version decision, not something to do silently as a side effect.

## Known Gaps / Honest Caveats

1. **No GUI click-through testing yet.** Everything above was verified by compiling, reading the code paths, and checking scanner logic against a real test repo's `/build/scripts/` folder — not by clicking buttons in the running app. Worth doing before calling this release-ready.
2. **"Total Builds Run" is all-time, not session-scoped.** The requirements doc (§4.1) describes a per-session counter; this app persists history across restarts and counts everything, which is arguably more useful but is a deliberate deviation worth confirming with you.
3. **GitHub repos can be scanned but not built.** Running `bash` against a GitHub URL isn't possible without a local checkout, which the requirements doc doesn't fully address either — flagging it here as an implicit product decision.

## Decision Overrides In Effect

1. Stack: native Swift/SwiftUI, not Tauri/Rust/React — every phase above is built natively.
2. Storage: `projects.json` under `LXC-BRM-build-release/` for the config template; the live app's own recent-repos/history JSON lives in `~/Library/Application Support/LXC-BRM/`, not in the repo.
3. Any new PDF-vs-decision conflict gets recorded in `LXC-BRM-context/decisions/` before being built around.

## Tracking

| Phase | Checked / Total | Status |
| --- | --- | --- |
| 0 — Workspace Foundation | 5 / 5 | Done |
| 1 — Repository Input & Detection | 9 / 9 | Done |
| 2 — Build Execution & Management | 5 / 5 | Done (not yet GUI-tested) |
| 3 — Log Storage & Retrieval | 6 / 6 | Done (not yet GUI-tested) |
| 4 — Project & Build Overview | 5 / 5 | Done (not yet GUI-tested) |
| 5 — Multi-Repository Support | 5 / 5 | Done |
| 6 — Non-Functional Hardening & v1 Check | 3 / 6 | In Progress |
| 7 — Packaging & Deliverables | 3 / 4 | In Progress |
| **Total** | **41 / 45** | **~91% of checklist items checked** |

# todo-2026-08-16

All active todo tracking lives here. Other BRM folders should link back to this file instead of keeping their own daily todo files.

## Tasks

A. Build-release requirements.
1. Keep the build scripts in `LXC-BRM/BRM/LXC-BRM-build-release/scripts/`.
2. Keep `build-ios.sh`, `build-android.sh`, and `release.sh` available.
3. Keep the build command path visible in the README and worklog.
4. Keep script references easy to follow from the repository index.

B. Build logs and outputs.
1. Keep logs in `LXC-BRM/BRM/LXC-BRM-build-release/logs/`.
2. Use timestamped log names such as `build-2026-08-16-14-32-45.log`.
3. Preserve `.txt` export support when needed.
4. Keep the app build output separate from source folders.
5. Stage the final release package in `LXC-BRM/BRM/LXC-BRM-build-release/version/`.
6. Place the final `.dmg` in the `version/` folder.

C. Project mapping and folder structure.
1. Keep `projects.json` in the build-release area.
2. Map each repo to its local workspace path.
3. Keep the `LXC-BRM/` root as the native macOS app container.
4. Keep the BRM workspace under `LXC-BRM/BRM/`.
5. Keep shared, frameworks, build-release, worklog, and context as the named BRM areas.

D. Context and decision tracking.
1. Keep rules in `LXC-BRM/BRM/LXC-BRM-context/rules-context.md`.
2. Keep architecture in `LXC-BRM/BRM/LXC-BRM-context/architecture.md`.
3. Keep dated decisions in `LXC-BRM/BRM/LXC-BRM-context/decisions/`.
4. Record when a PDF requirement is overridden by a decision.
5. Update the context when rules, architecture, or decisions change.

E. Documentation and tracking.
1. Keep the root README and BRM README as index pages.
2. Keep the worklog README as the only active todo hub.
3. Keep the worklog summary current after todo changes.
4. Use this file for task, subtask, and tracking updates only.

F. Verification and sync.
1. Confirm the macOS app remains Xcode-openable.
2. Confirm the repo stays native macOS only.
3. Confirm the release path still ends in `.dmg` under `version/`.
4. Confirm the repository is committed and pushed to GitHub after changes.

## Tracking

| Item | Status | Notes |
| --- | --- | --- |
| A | Open | Build scripts and script availability |
| B | Open | Logs, outputs, and `.dmg` staging |
| C | Open | Project mapping and folder structure |
| D | Open | Rules, architecture, and decisions |
| E | Open | Documentation and todo centralization |
| F | Open | Verification and GitHub sync |
